# Docker Compose environment — copy to .env and fill in
# Never commit .env to git

# Jetstream2 pons1 instance — ask a team member for the IP
PONS1_IP=

# Path to your SSH key that is authorized on pons1
SSH_KEY_PATH=~/.ssh/jetstream2
